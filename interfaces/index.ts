export type Projects = {
    cover_image: object
    image_gallery: {images: [{alt: string}]}
    name: string
    publishedAt: string
    slug: object
    body: [object]
    summary: [object]
    _id: string
    image: string
}